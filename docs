# Godot 4 打包 Android APK 教程

## 前置准备
1. 安装 Godot 4 正式版
2. 配置 Android SDK & JDK（Godot 编辑器内一键安装）

## 导出步骤
1. 打开项目 → 顶部菜单 `项目` → `导出`
2. 点击 `添加` → 选择 `Android`
3. 基础设置
   - 应用名称：自定义游戏名
   - 包名：格式 `com.你的名字.游戏名`
   - 版本号：1
4. 密钥配置
   - 首次打包：选择「生成新密钥」
   - 填写密码并保存密钥文件
5. 屏幕设置
   - 屏幕方向：横屏 / 竖屏（按需选择）
6. 点击 `导出项目`，选择保存路径
7. 等待编译完成，得到 `.apk` 文件，可直接在安卓手机安装

## 注意事项
- 必须开启虚拟摇杆才能在手机操作
- 包名不可重复
- 密钥请妥善保存，后续更新包需要同一份密钥

